<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext stays off everywhere by default, including release builds.
         The exemption below covers only the loopback addresses used for local
         development: 10.0.2.2 is the emulator's alias for the host machine. A
         real deployment must be HTTPS and needs no entry here. -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
